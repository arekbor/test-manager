version: "3.8"

services:
  db:
    image: postgres
    restart: always
    volumes:
      - db:/var/lib/postgresql/data
    env_file:
      - .env.prod

  web:
    build: .
    ports:
      - ${MAP_PORT}:80
    depends_on:
      - db
    env_file:
      - .env.prod

volumes:
  db:
    driver: local
