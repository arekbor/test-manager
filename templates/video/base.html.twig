{% extends 'base.html.twig' %}

{% block body %}
    <div class="mt-2">
        <a 
            href="{{ path('app_module_details', { id: moduleId }) }}" 
            class="btn btn-primary" 
            role="button"
        >
            {{ 'templates.video.upload.module'|trans }}
        </a>

        <div class="mt-3">
            {% block wrapper %}{% endblock %}
        </div>
    </div>
{% endblock %}