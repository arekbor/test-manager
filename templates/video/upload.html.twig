{% extends 'base.html.twig' %}

{% block title %} 
    {{ 'title.upload.video'|trans }} 
{% endblock %}

{% block body %}
    <div class="mt-2">
        <a 
            href="{{ path('app_module_details', { id: moduleId }) }}" 
            class="btn btn-primary" 
            role="button"
        >
            {{ 'video.upload.go.to.module.button'|trans }}
        </a>
        
        <div class="mt-3">
            {{ component('VideoUpload', { moduleId: moduleId, defer: true }) }}
        </div>
    </div>
{% endblock %}
