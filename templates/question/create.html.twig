{% extends 'base.html.twig' %}

{% block title %} 
    {{ 'templates.question.create.title'|trans }} 
{% endblock %}

{% block body %}
    <div class="mt-2">
        {{ component('QuestionForm', { moduleProp: module, defer: true }) }}
    </div>
{% endblock %}
