<div 
    {{ attributes }}
    class="d-flex flex-column align-items-center"
>
    <h1 class="text-danger display-4 mt-5">
        {{ header|trans }}
    </h1>

    <p class="text-muted">
        {{ content|trans }}
    </p>
</div>