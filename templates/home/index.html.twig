{% extends 'base.html.twig' %}

{% block body %}
    <div class="mt-2">
        {{ component('HeaderBreakLine', { title: 'module.data.table.title'|trans, defer: true }) }}
        {{ data_table_action_bar(modules) }}
        {{ data_table_table(modules) }}
        <div class="mt-3">
            {{ data_table_pagination(modules) }}
        </div>
    </div>
{% endblock %}
